const Users = () => {
    return (
        <div style={userStyle}>
        </div>
    );

    const userStyle = {
        display: 'grid',
        gridTemplateColumns: 'repeat(3, 1fr)',
        gridGap: '1rem'
      };
      
}